import {RoleType} from "./RoleType";

export class User {
		email: string;
		firstName: string;
		id: number;
		internal: boolean;
		lastName: string;
		password: string;
		role: RoleType;
    imageUri: string;
    image: string;
}
