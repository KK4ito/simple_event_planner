import {Role} from "./Role";

export class User {
		email: string;
		firstName: string;
		id: number;
		internal: boolean;
		lastName: string;
		password: string;
		role: Role;
    imageUri: string;
    image: string;
}
